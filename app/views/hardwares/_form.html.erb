<%= form_with(model: hardware, url: url, local: true) do |form| %>
  <% if hardware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hardware.errors.count, "error") %> prohibited this hardware item from being saved:</h2>

      <ul>
      <% hardware.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vendor %>
    <%= form.text_field :vendor, required: true %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model, required: true  %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, required: true %>
  </div>

  <div class="actions">
    <%= form.submit hardware.new_record? ? 'Create hardware item' : 'Save changes' %>
  </div>
<% end %>
