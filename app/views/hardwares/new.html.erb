<% title "New hardware item on #{@event.name}" %>
<% page_sm %>

<%= render 'events/nav', event: @event, active: 'hardware' %>

<h1>
  Add a hardware item
</h1>

<%= render 'form', url: event_hardwares_path(@event), hardware: @hardware %>
