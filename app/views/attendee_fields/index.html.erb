<% title "Fields on #{@event.name}" %>
<div class="breadcrumbs">
  <%= link_to @event.name, @event %> › <%= link_to 'Attendees', event_attendees_path %>
</div>

<h1 class="heading">
  Fields
  <%= link_to 'Add a field', new_event_attendee_field_path, class: 'btn' %>
</h1>

<%= react_component 'Fields', props: { event: @event.as_json } %>
