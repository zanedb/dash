<%= form_with(model: attendee_field, url: url, local: true) do |form| %>
  <% if attendee_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(attendee_field.errors.count, "error") %> prohibited this field from being saved:</h2>

      <ul>
        <% attendee_field.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :label %>
    <%= form.text_field :label %>
  </div>

  <div class="field">
    <%= form.label :kind %>
    <%= form.select :kind, AttendeeField.kinds.keys %>
  </div>

  <div class="field">
    <%= form.label :options %>
    <%= form.text_area :options %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
