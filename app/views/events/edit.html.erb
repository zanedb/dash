<% title "Editing #{@event.name}" %>
<div class="breadcrumbs">
  <%= link_to 'Home', root_url %> › <%= link_to @event.name, @event %>
</div>
<h1><%= @event.name %></h1>

<%= render 'form', event: @event %>
