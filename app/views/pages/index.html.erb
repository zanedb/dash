<% if user_signed_in? %>
  <% title 'Home' %>
  <h1>H&M</h1>
  <h3 class="a-lil-bottom-margin">✨ A next-gen hackathon management system</h3>
  <h4 class="a-lil-bottom-margin">
    👋 Welcome, <%= current_user.name %>. Happy <%= local_time(Time.now, '%A') %>! You’re doing great.
  </h4>
  <p class="slate">(Please note: <u>H&M</u> is in no way the final name. Consider it a codename.)</p>

  <% if @invites.any? %>
    <h2>
      Invitations
    </h2>
    <p class="a-lil-bottom-margin slate">You have <%= pluralize @invites.count, 'pending invitation' %>.</p>
    <ul class="list list--border-top">
      <% @invites.each do |invite| %>
        <li><%= link_to invite.event.name, event_organizer_position_invite_path(invite.event, invite) %></li>
      <% end %>
    </ul>
  <% end %>
  <% if @events.any? %>
    <h2>
      My Events
    </h2>
    <ul class="list">
      <% @events.each do |event| %>
        <%= link_to event do %>
          <li>
            <div>
              <strong style="display: block;">
                <%= event.name %>
              </strong>
              <span class="muted">
                <%= event.city %> — <%= event_length(event).html_safe %>
              </span>
            </div>
          </li>
        <% end %>
      <% end %>
    </ul>
  <% end %>
  <% if current_user.admin? %>
    <h2>Grand Admin Station</h2>
    <ul class="list">
      <li class="h3"><%= link_to 'All Events', events_path %></li>
      <li class="h3"><%= link_to 'New Event', new_event_path %></li>
      <li class="h3"><%= link_to 'All Users', all_users_path %></li>
    </ul>
  <% end %>
<% else %>
  <h1>There’s more to see…</h1>
  <article>
    <%= link_to 'Sign in', new_user_session_path, class: 'btn' %>
  </article>
<% end %>
