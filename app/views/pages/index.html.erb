
<div class="breadcrumbs">
  <a href="/">Home</a>
</div>
<h1>H&M</h1>
<h3 class="a-lil-bottom-margin">✨ A next-gen hackathon management system</h3>
<% if user_signed_in? %>
  <h4 class="a-lil-bottom-margin">
    👋 Welcome, <%= current_user.name %>. You’re doing great.
    <% if current_user.admin? %>
      You’re also an <span style="color: red;">admin</span>, hellooooo.
    <% end %>
  </h4>
  <%= link_to 'View events', events_path %>
  <br />
  <%= link_to 'Admin dashboard', admin_path %>
  <br />
  <%= link_to 'My account', edit_user_registration_path %>
  <br />
  <%= link_to 'Log out', destroy_user_session_path, :method => :delete %>
  <% else %>
  <h2><%= link_to 'Login', new_user_session_path %></h2>
<% end %>
