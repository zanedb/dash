<%= form_with(model: webhook, url: url, local: true) do |form| %>
  <% if webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(webhook.errors.count, "error") %> prohibited this webhook from being saved:</h2>

      <ul>
      <% webhook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, required: true %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, required: true  %>
  </div>

  <div class="field">
    <%= form.label :request_type %>
    <%= form.select :request_type, ['POST','GET'], required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
