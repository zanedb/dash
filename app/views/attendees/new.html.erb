<% title "New Attendee on #{@event.name}" %>
<div class="breadcrumbs">
  <%= link_to 'Home', root_url %> › <%= link_to 'Events', events_url %> › <%= link_to @event.name, @event %> › <%= link_to 'Attendees', event_attendees_path %>
</div>

<h1>
  <%= @event.name %>
  <br><span style="font-size: 2rem;">Add an Attendee</span>
</h1>

<%= render 'form', url: event_attendees_path, attendee: @attendee %>
